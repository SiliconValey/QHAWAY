# QHAWAY — dependencias
# Instalar con:  pip install -r requirements.txt

# Interfaz gráfica (Etapa 7).
PySide6>=6.6

# Informes HTML→PDF (Etapa 8).
jinja2>=3.1
weasyprint>=60          # requiere Pango/cairo del sistema (o se congela con PyInstaller)

# Conector de IA (Etapa 4). Se importa como 'anthropic'.
anthropic>=0.40

# Extracción de contenido (Etapa 3).
pymupdf>=1.24          # se importa como 'fitz'
python-docx>=1.1       # se importa como 'docx'
pyyaml>=6.0            # se importa como 'yaml'

# Desarrollo / tests.
pytest>=8.0
pytest-qt>=4.4       # tests de humo de la UI (headless con QT_QPA_PLATFORM=offscreen)
